// Persistent Bugger

function persistence(num) {
   //code me

  // parse number into digits and multiply, length >1?
  while(num.toString().length > 1) {
    num.to
  } // add to counter

  // return counter
  return i
}
